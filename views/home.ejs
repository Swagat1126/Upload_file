<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.css" rel="stylesheet" />
  <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  <title>File Upload</title>
</head>

<body class="bg-gray-100 dark:bg-gray-900 text-gray-800 dark:text-white relative">
  
  <nav class="absolute top-0 left-0 w-full p-4 flex justify-between items-center">
    <h2 class="text-xl font-bold text-white">FileUploader</h2>
    <div>
      <a href="/login" class="text-white hover:text-blue-300 mr-4">Login</a>
      <a href="/register" class="text-white hover:text-blue-300">Register</a>
    </div>
  </nav>

  
  <button onclick="toggleTheme()" class="absolute top-4 right-4 bg-gray-200 dark:bg-gray-800 text-black dark:text-white px-3 py-1 rounded">
    Toggle Theme
  </button>

  
  <main class="h-screen flex flex-col items-center justify-center text-center px-4">
    <img src="/images/upload-illustration.png" alt="Upload" class="w-48 mb-6" />
    <h1 class="text-4xl font-bold mb-4">Welcome to File Upload</h1>
    <p class="text-gray-500 dark:text-gray-300 max-w-xl mb-6">
      Upload and manage your files easily with our simple and secure platform. Fast, safe, and free.
    </p>
    <button onclick="showPopUP()" class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-3 rounded-lg shadow">
      Upload File
    </button>
  </main>

  
  <div id="uploadModal" class="hidden fixed inset-0 bg-black bg-opacity-50 backdrop-blur-sm flex items-center justify-center z-50">
    <div class="bg-white dark:bg-gray-800 rounded-xl p-8 w-full max-w-md shadow-xl relative">
      <form action="/upload" method="POST" enctype="multipart/form-data">
        <label for="dropzone-file" class="block text-sm font-medium mb-4">Upload your file</label>
        <label for="dropzone-file" class="flex flex-col items-center justify-center w-full h-48 border-2 border-dashed border-gray-300 rounded-lg cursor-pointer bg-gray-50 dark:bg-gray-700 hover:bg-gray-100 dark:hover:bg-gray-600">
          <div class="flex flex-col items-center justify-center pt-5 pb-6">
            <i class="ri-upload-cloud-2-line text-3xl mb-2"></i>
            <p class="text-sm text-gray-500 dark:text-gray-300"><span class="font-semibold">Click to upload</span> or drag and drop</p>
            <p class="text-xs text-gray-500 dark:text-gray-400">PNG, JPG, PDF (Max 5MB)</p>
          </div>
          <input id="dropzone-file" type="file" name="file" class="hidden" />
        </label>
        <button type="submit" class="w-full mt-6 bg-blue-600 hover:bg-blue-700 text-white py-2 rounded-lg font-semibold">Upload</button>
      </form>
      <button onclick="hidePopUp()" class="absolute top-2 right-2 text-gray-500 hover:text-red-500">
        <i class="ri-close-line text-xl"></i>
      </button>
    </div>
  </div>

 
  <footer class="absolute bottom-4 text-sm text-gray-400 text-center w-full">
    &copy; 2025 File Upload App. All rights reserved.
  </footer>

 
  <script>
    function showPopUP() {
      document.getElementById('uploadModal').classList.remove('hidden');
    }
    function hidePopUp() {
      document.getElementById('uploadModal').classList.add('hidden');
    }
    function toggleTheme() {
      document.documentElement.classList.toggle('dark');
    }
  </script>
  <script src="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.js"></script>
</body>
</html>
